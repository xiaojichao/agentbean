import{describe,expect,test}from'vitest';import{evaluateCancellationPreconditions,evaluateClosurePreconditions,evaluateRootCascadeCloseout}from'../src/task-termination-policy.js';
describe('cancellation',()=>{test('allows non-terminal',()=>{expect(evaluateCancellationPreconditions('todo')).toEqual({kind:'allowed'});});test('rejects terminal',()=>{expect(evaluateCancellationPreconditions('done')).toEqual({kind:'rejected',reason:'already-terminal'});expect(evaluateCancellationPreconditions('cancelled')).toEqual({kind:'rejected',reason:'already-terminal'});});});
describe('closure',()=>{test('allows non-terminal',()=>{expect(evaluateClosurePreconditions('todo')).toEqual({kind:'allowed'});});test('rejects closed',()=>{expect(evaluateClosurePreconditions('closed')).toEqual({kind:'rejected',reason:'already-terminal'});});});
describe('cascade',()=>{test('identifies affected subtasks',()=>{const r=evaluateRootCascadeCloseout('in_progress','cancelled',[{taskId:'s1',status:'todo',hasActiveClaim:true,hasActiveGrant:false,claimLeaseId:'l1'},{taskId:'s2',status:'done',hasActiveClaim:false,hasActiveGrant:false}]);expect(r.kind).toBe('cascade_allowed');expect(r.affectedSubtaskIds).toEqual(['s1']);expect(r.claimLeaseIdsToRevoke).toEqual(['l1']);});test('rejects terminal root',()=>{expect(evaluateRootCascadeCloseout('cancelled','closed',[]).kind).toBe('rejected');});});
