---
status: accepted
---

# PI Manager 可以自动创建低风险 Tracked task

当请求意图明确并需要持续跟踪、异步等待、多 Agent 协作、明确交付或用户审核时，PI Manager 可以自动创建 Tracked task，并以紧凑状态告知用户且允许立即取消或修改。闲聊、简单问答和一次性短回复不创建 Task；高成本、高风险、不可逆、跨越当前隐私边界或意图不清的请求必须先询问用户。该门槛在减少机械确认的同时，避免把每条消息都转化为持久工作或越权执行。
