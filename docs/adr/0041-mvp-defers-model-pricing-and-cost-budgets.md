---
status: accepted
---

# MVP 暂不考虑模型费用

PI MVP 不配置输入/输出 Token 单价，不进行人民币或其他货币换算，不估算 Run 费用，也不提供 Team 月度金额上限。Provider Card、Active PI Model 和 Team PI 设置中都不出现价格、账单或货币预算字段。

此前治理模型中的成本边界保留为未来能力，但不约束本次 MVP 实现。等真实费用治理需求出现后，再单独决定价格来源、币种、套餐、汇率、估算与最终账单之间的关系，避免在首版引入未经需要证明的财务模型。
