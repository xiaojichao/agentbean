---
status: accepted
---

# PI Manager 使用 AgentBean 系统协调身份

PI Manager 不作为普通 Agent 出现在 Team 成员或 Agent 列表中，也不把自己的协调内容伪装成外部 Agent 回复。需要追问、报告结构化协调状态或提交多 Agent 汇总时，它以 AgentBean 系统身份发言；外部 Agent 的原始回复继续归属于实际执行者，简单单 Agent 请求中 PI 保持不可见。用户可以按需展开选择与建 Task 的审计解释，但不展示 PI 内部推理。
