# 设备页：区分当前设备与远程设备 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 web-next 设备页按 `device.isLocal` 区分本机/远程设备：标识 `[本机]` badge、远程目录浏览降级为手动填路径、远程删除加输入设备名护栏、未关联本机设备时给引导提示。

**Architecture:** 在 `lib/device-permissions.ts` 抽 3 个纯判定函数作为唯一"本机/远程"出口；`devices/page.tsx` 5 处（列表项、详情头部、目录浏览按钮、删除对话框、页面顶部）引用 `device.isLocal` 分流。纯前端，不动 server-next / daemon / 授权模型。

**Tech Stack:** Next.js App Router, React, TypeScript, vitest, zustand, lucide-react

## Global Constraints

- 纯前端 `apps/web-next`；不动 `apps/server-next`、`apps/daemon-next`、授权模型。
- 判定键统一用 `device.isLocal`（DeviceDto 已有字段；`DeviceDetail` 内 `isLocalDevice` 局部变量已在 page.tsx:403 存在）。
- badge 样式沿用现有「可升级」badge（page.tsx:305），改绿色：`rounded bg-green-50 px-1 text-[10px] font-medium text-green-700`。
- 文案中文。
- web-next 测试惯例（[[web-next-test-conventions]]）：`tests/` 只测 lib 纯函数、不测 `page.tsx` 组件、无 jsdom。故 Task 1 严格 TDD；Task 2–5（UI 改动）验证用类型检查 + 手动，不写组件测试。
- 运行单测：`cd apps/web-next && npx vitest run tests/device-permissions.test.ts`
- 类型检查：`cd apps/web-next && npx tsc --noEmit`

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `apps/web-next/lib/device-permissions.ts` | 本机/远程纯判定函数 | 新增 3 个导出函数 |
| `apps/web-next/tests/device-permissions.test.ts` | 纯函数单测 | 新增 1 个 describe（3 个 test） |
| `apps/web-next/app/[networkPath]/devices/page.tsx` | 设备页 UI | 5 处改动（见 Task 2–5） |

---

### Task 1: 本机/远程判定纯函数（TDD）

**Files:**
- Modify: `apps/web-next/lib/device-permissions.ts`
- Test: `apps/web-next/tests/device-permissions.test.ts`

**Interfaces:**
- Produces:
  - `canBrowseDirectory(isLocal: boolean | null | undefined): boolean` —— 仅本机返回 true
  - `requiresDeleteNameConfirm(isLocal: boolean | null | undefined): boolean` —— 远程返回 true
  - `hasLocalDevice(devices: { isLocal?: boolean | null }[]): boolean` —— 列表含本机返回 true

- [ ] **Step 1: 写失败测试**

修改 `apps/web-next/tests/device-permissions.test.ts` 第 2 行 import，替换为：

```ts
import { canAddCustomAgentToDevice, canBrowseDirectory, canManageDeviceForUser, hasLocalDevice, requiresDeleteNameConfirm } from '../lib/device-permissions';
```

在文件末尾（第 66 行 `});` 之后）追加：

```ts
describe('device local/remote distinction (本机/远程分流)', () => {
  test('canBrowseDirectory: 仅本机可浏览目录，远程降级手动填', () => {
    expect(canBrowseDirectory(true)).toBe(true);
    expect(canBrowseDirectory(false)).toBe(false);
    expect(canBrowseDirectory(undefined)).toBe(false);
    expect(canBrowseDirectory(null)).toBe(false);
  });

  test('requiresDeleteNameConfirm: 远程删除需输入设备名，本机无需', () => {
    expect(requiresDeleteNameConfirm(true)).toBe(false);
    expect(requiresDeleteNameConfirm(false)).toBe(true);
    expect(requiresDeleteNameConfirm(undefined)).toBe(true);
  });

  test('hasLocalDevice: 含本机设备为真，全远程/空为假', () => {
    expect(hasLocalDevice([{ isLocal: true }, { isLocal: false }])).toBe(true);
    expect(hasLocalDevice([{ isLocal: false }, { isLocal: undefined }])).toBe(false);
    expect(hasLocalDevice([])).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd apps/web-next && npx vitest run tests/device-permissions.test.ts`
Expected: FAIL —— `canBrowseDirectory is not defined`（函数尚未实现）。

- [ ] **Step 3: 实现 3 个纯函数**

在 `apps/web-next/lib/device-permissions.ts` 末尾（第 31 行 `}` 之后）追加：

```ts

// 远程设备（非本机）目录浏览：本机依赖 GUI 目录选择器，远程 daemon 多无桌面会话会挂起。
// 远程一律降级为手动填写项目绝对路径（isLocal !== true 视为远程，含未关联本机设备场景）。
export function canBrowseDirectory(isLocal: boolean | null | undefined): boolean {
  return isLocal === true;
}

// 远程设备删除加额外护栏（输入设备名确认）；本机维持现有确认。
export function requiresDeleteNameConfirm(isLocal: boolean | null | undefined): boolean {
  return isLocal !== true;
}

// 设备列表中是否存在本机设备（用于「未关联本机设备」引导提示）。
export function hasLocalDevice(devices: { isLocal?: boolean | null }[]): boolean {
  return devices.some((d) => d.isLocal === true);
}
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd apps/web-next && npx vitest run tests/device-permissions.test.ts`
Expected: PASS（全部测试通过，含原有 6 个 + 新增 3 个）。

- [ ] **Step 5: Commit**

```bash
git add apps/web-next/lib/device-permissions.ts apps/web-next/tests/device-permissions.test.ts
git commit -m "feat(web-next): 新增本机/远程设备判定纯函数" -m "canBrowseDirectory/requiresDeleteNameConfirm/hasLocalDevice，供设备页 UI 分流。" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: `[本机]` badge 视觉标识

**Files:**
- Modify: `apps/web-next/app/[networkPath]/devices/page.tsx`（设备列表项设备名 ~300；`DeviceDetail` h1 ~569）

**Interfaces:**
- Consumes: `device.isLocal`（列表项 device 来自 `deviceGroups`，DeviceDto 含 isLocal）；`DeviceDetail` 内已有 `isLocalDevice`（403）。

- [ ] **Step 1: 列表项设备名旁加 badge**

将 `devices/page.tsx` 第 300 行：

```tsx
                    <div className="truncate text-sm font-medium leading-tight">{deviceDisplayName(device) === device.id ? '未命名设备' : deviceDisplayName(device)}</div>
```

替换为：

```tsx
                    <div className="flex items-center gap-1">
                      <span className="truncate text-sm font-medium leading-tight">{deviceDisplayName(device) === device.id ? '未命名设备' : deviceDisplayName(device)}</span>
                      {device.isLocal === true && (
                        <span className="shrink-0 rounded bg-green-50 px-1 text-[10px] font-medium text-green-700">本机</span>
                      )}
                    </div>
```

- [ ] **Step 2: `DeviceDetail` 头部 h1 加 badge**

将 `devices/page.tsx` 第 569 行：

```tsx
            <h1 className="text-lg font-semibold">{displayName}</h1>
```

替换为：

```tsx
            <h1 className="flex items-center gap-2 text-lg font-semibold">
              <span>{displayName}</span>
              {isLocalDevice && (
                <span className="rounded bg-green-50 px-1.5 py-0.5 text-[10px] font-medium text-green-700">本机</span>
              )}
            </h1>
```

- [ ] **Step 3: 类型检查**

Run: `cd apps/web-next && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 4: 手动验证（可选，需 dev 环境）**

启动 `cd apps/web-next && npm run dev`（端口 4101，需 server-next 运行）。在设备页：
- 本机设备（做过 device-login 的那台）：列表项与详情头部均显示绿色 `[本机]` badge。
- 远程设备：不显示 badge。

- [ ] **Step 5: Commit**

```bash
git add apps/web-next/app/[networkPath]/devices/page.tsx
git commit -m "feat(web-next): 设备列表与详情头部加 [本机] badge" -m "按 device.isLocal 标识当前正在使用的本机设备，区分名下远程设备。" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 远程目录浏览降级（方案 A）

**Files:**
- Modify: `apps/web-next/app/[networkPath]/devices/page.tsx`
  - `DirectoryBrowseButton` 组件签名 + 渲染（90–173）
  - `AgentConfigDialog` 内使用点（1372）
  - `AddCustomAgentDialog` 组件签名（1400）
  - `AddCustomAgentDialog` 调用点（783）
  - `AddCustomAgentDialog` 内使用点（1496）

**Interfaces:**
- Consumes: Task 1 的 `canBrowseDirectory`（组件内直接用 `isLocal === false` 判定即可，无需调用纯函数；纯函数留作外部可测试出口）。
- `DirectoryBrowseButton` 新增 prop `isLocal?: boolean`（默认 `true`，向后兼容）。
- `AddCustomAgentDialog` 新增 prop `isLocal?: boolean`（默认 `true`），由调用方 DeviceDetail 传入 `isLocalDevice`。

- [ ] **Step 1: `DirectoryBrowseButton` 签名加 `isLocal`**

将 `devices/page.tsx` 第 90–102 行：

```tsx
function DirectoryBrowseButton({
  onSelect,
  onError,
  deviceId,
  daemonVersion,
  disabled = false,
}: {
  onSelect: (path: string) => void;
  onError?: (message: string) => void;
  deviceId?: string;
  daemonVersion?: string | null;
  disabled?: boolean;
}) {
```

替换为：

```tsx
function DirectoryBrowseButton({
  onSelect,
  onError,
  deviceId,
  daemonVersion,
  disabled = false,
  isLocal = true,
}: {
  onSelect: (path: string) => void;
  onError?: (message: string) => void;
  deviceId?: string;
  daemonVersion?: string | null;
  disabled?: boolean;
  isLocal?: boolean;
}) {
```

- [ ] **Step 2: `DirectoryBrowseButton` 渲染加远程分支**

在 `return (`（第 147 行）之前插入远程分支。把下面「替换前」整段换成「替换后」整段（紧随其后的 `<input ... />` 隐藏文件输入维持不变）：

**替换前：**

```tsx
  return (
    <>
      <button type="button" disabled={disabled || browsing} onClick={browse} className="shrink-0 flex items-center gap-1 rounded-md border border-neutral-200 px-3 py-1.5 text-xs text-neutral-600 hover:bg-neutral-50 disabled:cursor-not-allowed disabled:opacity-50">
        <FolderOpen size={12} /> {browsing ? '等待选择...' : '浏览'}
      </button>
```

**替换后：**

```tsx
  if (isLocal === false) {
    return (
      <span className="shrink-0 self-center text-[11px] text-neutral-400">
        远程设备请手动填写该设备上的项目绝对路径
      </span>
    );
  }
  return (
    <>
      <button type="button" disabled={disabled || browsing} onClick={browse} className="shrink-0 flex items-center gap-1 rounded-md border border-neutral-200 px-3 py-1.5 text-xs text-neutral-600 hover:bg-neutral-50 disabled:cursor-not-allowed disabled:opacity-50">
        <FolderOpen size={12} /> {browsing ? '等待选择...' : '浏览'}
      </button>
```

- [ ] **Step 3: `AgentConfigDialog` 使用点（1372）传 `isLocal`**

将第 1372 行：

```tsx
                  <DirectoryBrowseButton deviceId={agent.deviceId} daemonVersion={device?.systemInfo?.daemonVersion ?? device?.daemonVersionInfo?.current ?? null} onSelect={setCwd} onError={setError} />
```

替换为：

```tsx
                  <DirectoryBrowseButton deviceId={agent.deviceId} daemonVersion={device?.systemInfo?.daemonVersion ?? device?.daemonVersionInfo?.current ?? null} onSelect={setCwd} onError={setError} isLocal={device?.isLocal === true} />
```

- [ ] **Step 4: `AddCustomAgentDialog` 签名（1400）加 `isLocal`**

将第 1400 行：

```tsx
function AddCustomAgentDialog({ deviceId, networkId, daemonVersion, runtimes, onClose, onCreated }: { deviceId: string; networkId?: string | null; daemonVersion?: string | null; runtimes: any[]; onClose: () => void; onCreated: () => void }) {
```

替换为：

```tsx
function AddCustomAgentDialog({ deviceId, networkId, daemonVersion, runtimes, isLocal = true, onClose, onCreated }: { deviceId: string; networkId?: string | null; daemonVersion?: string | null; runtimes: any[]; isLocal?: boolean; onClose: () => void; onCreated: () => void }) {
```

- [ ] **Step 5: 调用点（783–787）传 `isLocal`**

将第 783–787 行：

```tsx
        <AddCustomAgentDialog
          deviceId={device.id}
          networkId={currentTeamId}
          daemonVersion={device.systemInfo?.daemonVersion ?? device.daemonVersionInfo?.current ?? null}
          runtimes={runtimeList}
```

替换为：

```tsx
        <AddCustomAgentDialog
          deviceId={device.id}
          networkId={currentTeamId}
          daemonVersion={device.systemInfo?.daemonVersion ?? device.daemonVersionInfo?.current ?? null}
          runtimes={runtimeList}
          isLocal={isLocalDevice}
```

- [ ] **Step 6: `AddCustomAgentDialog` 内使用点（1496）传 `isLocal`**

将第 1496 行：

```tsx
              <DirectoryBrowseButton deviceId={deviceId} daemonVersion={daemonVersion} onSelect={setCwd} onError={setError} />
```

替换为：

```tsx
              <DirectoryBrowseButton deviceId={deviceId} daemonVersion={daemonVersion} onSelect={setCwd} onError={setError} isLocal={isLocal} />
```

- [ ] **Step 7: 类型检查**

Run: `cd apps/web-next && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 8: 手动验证（可选）**

dev 环境，给**远程设备**添加/配置 custom agent：项目目录处不显示「浏览」按钮，改为提示「远程设备请手动填写该设备上的项目绝对路径」，手动填路径可正常提交。本机设备「浏览」按钮正常弹窗。

- [ ] **Step 9: Commit**

```bash
git add apps/web-next/app/[networkPath]/devices/page.tsx
git commit -m "feat(web-next): 远程设备目录浏览降级为手动填路径" -m "远程设备(isLocal=false)不渲染浏览按钮，避免 daemon 无桌面会话时前端挂起等待。" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 远程删除护栏（输入设备名确认）

**Files:**
- Modify: `apps/web-next/app/[networkPath]/devices/page.tsx`
  - 顶部 import（10）
  - `DeviceDetail` state（383–384 后）
  - 删除设备打开按钮（732）
  - 删除确认对话框（736–751）

**Interfaces:**
- Consumes: Task 1 的 `requiresDeleteNameConfirm(isLocal)`；`DeviceDetail` 内 `isLocalDevice`（403）、`displayName`（391）。

- [ ] **Step 1: import 补 `requiresDeleteNameConfirm`**

将第 10 行：

```tsx
import { canAddCustomAgentToDevice, canManageDeviceForUser } from '@/lib/device-permissions';
```

替换为：

```tsx
import { canAddCustomAgentToDevice, canManageDeviceForUser, requiresDeleteNameConfirm } from '@/lib/device-permissions';
```

- [ ] **Step 2: `DeviceDetail` 加确认名 state**

将第 383–384 行：

```tsx
  const [deviceDeleteSaving, setDeviceDeleteSaving] = useState(false);
  const [deviceDeleteError, setDeviceDeleteError] = useState('');
```

替换为：

```tsx
  const [deviceDeleteSaving, setDeviceDeleteSaving] = useState(false);
  const [deviceDeleteError, setDeviceDeleteError] = useState('');
  const [deviceDeleteConfirmName, setDeviceDeleteConfirmName] = useState('');
```

- [ ] **Step 3: 打开删除对话框时重置确认名**

将第 732 行：

```tsx
            <button onClick={() => setShowDeleteConfirm(true)} className="rounded-md border border-red-300 px-4 py-2 text-sm text-red-600 hover:bg-red-50" data-smoke="device-delete-open">
```

替换为：

```tsx
            <button onClick={() => { setDeviceDeleteConfirmName(''); setShowDeleteConfirm(true); }} className="rounded-md border border-red-300 px-4 py-2 text-sm text-red-600 hover:bg-red-50" data-smoke="device-delete-open">
```

- [ ] **Step 4: 删除确认对话框加远程输入设备名 + disabled 扩展**

将第 736–751 行整段：

```tsx
          {showDeleteConfirm && (
            <div className="mt-4 rounded-md border border-red-200 bg-red-50 p-4">
              <p className="mb-3 text-sm text-red-700">确定要删除设备 <strong>{displayName}</strong> 吗？此操作不可撤销。</p>
              {deviceDeleteError && (
                <div className="mb-3 rounded-md border border-red-200 bg-white px-3 py-2 text-xs text-red-700" data-smoke="device-delete-error">
                  {deviceDeleteError}
                </div>
              )}
              <div className="flex gap-2">
                <button onClick={() => { setDeviceDeleteError(''); setShowDeleteConfirm(false); }} disabled={deviceDeleteSaving} className="rounded-md border border-neutral-300 px-3 py-1.5 text-sm hover:bg-neutral-50 disabled:opacity-50">取消</button>
                <button onClick={confirmDeleteDevice} disabled={deviceDeleteSaving} className="rounded-md bg-red-600 px-3 py-1.5 text-sm text-white hover:bg-red-700 disabled:opacity-50" data-smoke="device-delete-confirm">
                  {deviceDeleteSaving ? '删除中...' : '确认删除'}
                </button>
              </div>
            </div>
          )}
```

替换为：

```tsx
          {showDeleteConfirm && (
            <div className="mt-4 rounded-md border border-red-200 bg-red-50 p-4">
              <p className="mb-3 text-sm text-red-700">确定要删除设备 <strong>{displayName}</strong> 吗？此操作不可撤销。</p>
              {requiresDeleteNameConfirm(isLocalDevice) && (
                <>
                  <p className="mb-2 text-xs font-medium text-red-600">这是远程设备，删除将影响该设备的 daemon 连接。请输入设备名 <strong>{displayName}</strong> 以确认：</p>
                  <input
                    value={deviceDeleteConfirmName}
                    onChange={(e) => setDeviceDeleteConfirmName(e.target.value)}
                    placeholder={displayName}
                    className="mb-3 w-full rounded-md border border-red-200 bg-white px-3 py-1.5 text-sm outline-none focus:border-red-400"
                    data-smoke="device-delete-name-input"
                  />
                </>
              )}
              {deviceDeleteError && (
                <div className="mb-3 rounded-md border border-red-200 bg-white px-3 py-2 text-xs text-red-700" data-smoke="device-delete-error">
                  {deviceDeleteError}
                </div>
              )}
              <div className="flex gap-2">
                <button onClick={() => { setDeviceDeleteError(''); setDeviceDeleteConfirmName(''); setShowDeleteConfirm(false); }} disabled={deviceDeleteSaving} className="rounded-md border border-neutral-300 px-3 py-1.5 text-sm hover:bg-neutral-50 disabled:opacity-50">取消</button>
                <button
                  onClick={confirmDeleteDevice}
                  disabled={deviceDeleteSaving || (requiresDeleteNameConfirm(isLocalDevice) && deviceDeleteConfirmName !== displayName)}
                  className="rounded-md bg-red-600 px-3 py-1.5 text-sm text-white hover:bg-red-700 disabled:opacity-50"
                  data-smoke="device-delete-confirm"
                >
                  {deviceDeleteSaving ? '删除中...' : '确认删除'}
                </button>
              </div>
            </div>
          )}
```

- [ ] **Step 5: 类型检查**

Run: `cd apps/web-next && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 6: 手动验证（可选）**

dev 环境：
- 远程设备删除：确认框要求输入设备名，输入不匹配时「确认删除」禁用，匹配后可点。
- 本机设备删除：维持原「确定要删除吗」确认，无需输入名。

- [ ] **Step 7: Commit**

```bash
git add apps/web-next/app/[networkPath]/devices/page.tsx
git commit -m "feat(web-next): 远程设备删除加输入设备名护栏" -m "远程设备(isLocal=false)删除需输入设备名确认；本机维持现有确认。防误删不熟悉的远程设备。" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 未关联本机设备引导提示

**Files:**
- Modify: `apps/web-next/app/[networkPath]/devices/page.tsx`
  - `DevicesPage` 内 `deviceList` 计算后（207 后）
  - 左侧设备列表滚动容器前（278 前）

**Interfaces:**
- Consumes: Task 1 的 `hasLocalDevice(devices)`；`DevicesPage` 内 `deviceList`（207）。

**注：** spec 写了「[前往设备登录]」链接，但 `/device-login` 是 `[code]` 动态路由（无 index 页），web 端无 inviteCode 可拼，故改为纯文字引导（实现纠正 spec）。

- [ ] **Step 1: import 补 `hasLocalDevice` 并计算 `noLocalDevice`**

先补 import。将第 10 行（Task 4 已改为含 `requiresDeleteNameConfirm`）：

```tsx
import { canAddCustomAgentToDevice, canManageDeviceForUser, requiresDeleteNameConfirm } from '@/lib/device-permissions';
```

替换为：

```tsx
import { canAddCustomAgentToDevice, canManageDeviceForUser, hasLocalDevice, requiresDeleteNameConfirm } from '@/lib/device-permissions';
```

再计算 `noLocalDevice`。将第 207 行：

```tsx
  const deviceList = useMemo(() => Object.values(devices).sort(compareDevices), [devices]);
```

替换为：

```tsx
  const deviceList = useMemo(() => Object.values(devices).sort(compareDevices), [devices]);
  const noLocalDevice = !hasLocalDevice(deviceList);
```

- [ ] **Step 2: 左侧列表容器前插入提示**

定位第 278 行 `<div className="flex-1 overflow-y-auto p-1.5">`（设备列表滚动容器）。在该行**正前方**插入：

```tsx
              {noLocalDevice && deviceList.length > 0 && (
                <div className="mx-2 mb-2 rounded-md border border-amber-200 bg-amber-50 px-3 py-2 text-[11px] leading-relaxed text-amber-700">
                  未关联本机设备：目录浏览、删除确认等操作将按远程设备处理。请在当前使用的设备上完成设备登录以启用本机功能。
                </div>
              )}
              <div className="flex-1 overflow-y-auto p-1.5">
```

（即提示块紧贴列表容器上方；`deviceList.length > 0` 保证「暂无设备」空状态时不重复提示。）

- [ ] **Step 3: 类型检查**

Run: `cd apps/web-next && npx tsc --noEmit`
Expected: 无错误。

- [ ] **Step 4: 手动验证（可选）**

dev 环境，用**纯账号密码登录**（未 device-login）：设备列表上方显示琥珀色「未关联本机设备…」提示。做过 device-login 后提示消失。

- [ ] **Step 5: Commit**

```bash
git add apps/web-next/app/[networkPath]/devices/page.tsx
git commit -m "feat(web-next): 未关联本机设备时显示引导提示" -m "设备列表无 isLocal 设备(典型:账号密码登录未 device-login)时提示用户，操作将按远程设备处理。" -m "Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 验收

全部 5 个 task 完成后：
- `cd apps/web-next && npx vitest run` —— 全绿（含新增 3 个纯函数测试）。
- `cd apps/web-next && npx tsc --noEmit` —— 无错误。
- 手动（可选）：本机设备显示 `[本机]` badge；远程设备目录浏览降级为手动填、删除需输设备名；未 device-login 时顶部提示。
